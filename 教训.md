这次排查走了不少弯路。回顾下来，一个更高效的排查流程应该是：

---

## 优化后的排查 SOP

### 第一步：直接看 Traceback（不要猜）

```bash
# 触发请求后立刻看完整错误栈
docker logs --since 30s entropy-decrease-ai-gateway 2>&1 | tail -50
```

这次我们花了好几轮去猜"是模型名问题"、"是 max_tokens 问题"，但实际根因在第一次 Traceback 里就写清楚了：

```
httpx.HTTPStatusError: Client error '404 Not Found' for url '.../.well-known/jwks.json'
```

**原则：500 错误 → 先看栈，不要先猜配置。**

---

### 第二步：全局搜索，不要只改一处

当发现某个模式有问题时（如 URL 拼接缺 `/auth/v1`），立刻全局搜：

```bash
docker exec entropy-decrease-ai-gateway grep -rn "well-known" /app/ --include="*.py"
```

这次 `auth.py` 修了，但 `config.py` 还有一处同样的拼接，导致修了个寂寞。

**原则：修 pattern 时，grep 全局确认没有遗漏。**

---

### 第三步：验证运行时值，而非源文件

```bash
# 不要只 grep 源文件，直接打印运行时实际值
docker exec entropy-decrease-ai-gateway python -c "
from config import APP_CONFIG
print(APP_CONFIG.get('supabase_jwks_url'))
"
```

源文件正确 ≠ 运行时正确（可能有缓存、环境变量覆盖、多处拼接优先级问题）。

---

### 第四步：用时间窗口隔离日志

```bash
# 重启后只看新日志，避免旧日志干扰判断
docker logs --since 1m entropy-decrease-ai-gateway 2>&1
```

这次旧日志里的 404 让我们误以为修复没生效，实际是历史残留。

---

### 总结：四步法

| 步骤 | 动作 | 避免的坑 |
|------|------|----------|
| 1 | 看 Traceback | 不要凭经验猜原因 |
| 2 | 全局 grep 同类模式 | 不要只改一处就收工 |
| 3 | 打印运行时实际值 | 源文件正确 ≠ 运行正确 |
| 4 | `--since` 过滤日志 | 不要被旧日志误导 |

如果这次一开始就执行第 1 步看到 JWKS 404，再执行第 2 步全局 grep，两轮就能定位并修复，不需要来回 5-6 轮。