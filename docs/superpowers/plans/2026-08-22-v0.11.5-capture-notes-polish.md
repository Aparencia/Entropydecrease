# v0.11.5 采集体验与笔记打磨 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 v0.11.5 十一项采集体验与笔记打磨（A 采集体验 / B 数据过滤 / C 会话页 / D 笔记页），详见 [spec](../../superpowers/specs/2026-08-22-v0.11.x-capture-notes-polish-design.md)。

**Architecture:** 自底向上（AGENTS.md §3.6）——纯函数/原子层先行（B 组数据过滤 → A 组检测机制 → C 组会话页管线 → D 组前端），每任务 TDD（先写失败测试）。后端 Rust（Tauri command + 纯函数模块），前端 React（组件拆分遵循 ≤300 行）。

**Tech Stack:** Rust（rusqlite/tauri）/ TypeScript React / Vitest；测试隔离用内存库（`Db::open(":memory:")`），不依赖真实模型。

**执行顺序与依赖：** Phase 0 核实 → Phase 1（B：3/4/10，纯函数为主）→ Phase 2（A：11/1/2，其中 1️⃣ 依赖 2️⃣ 的信号结构）→ Phase 3（C：7→8→5→6，5️⃣ 依赖 8️⃣ 的锚点结果，6️⃣ 是最大块）→ Phase 4（D：9）。

---

## Phase 0：前置核实（归组落库率 + 环境基线）

### Task 0: 数据核实与基线确认

**Files:**
- 无代码改动；执行查询与构建验证

- [ ] **Step 1: 核实会话→笔记自动归组落库率**

执行（用 sqlite3 或临时 Rust 测试查询本机数据目录下的 entropy.db——路径见 tauri.conf.json / app_data_dir；测试库位置可问用户）：
```sql
SELECT COUNT(*) AS total, SUM(group_id IS NOT NULL) AS grouped, SUM(group_id IS NULL) AS ungrouped
FROM notes WHERE source = 'classroom';
```
判定：`ungrouped` 占比高 → 9️⃣② 走"路由信号增强"分支；占比低 → 走"UI 感知优化"分支。结果记入本任务 Step 2。

- [ ] **Step 2: 确认 cargo test 环境可用性**

运行：`cd app/src-tauri; cargo test --lib 2>&1 | tail -5`（工作区根为 `d:\Program own\aicode\work space\Entropydecrease`，注意目录名含空格——PowerShell 用引号）
预期：通过或出现 v0.11.4 记录的 0xc0000139（test harness exe 启动失败）。若失败，纯函数任务改用 `rustc --test` 独立编译验证（见各任务验证命令注明），并在最终验收时记录。

- [ ] **Step 3: 记录基线并继续**

将 Step 1/2 结果记入本文件末尾"执行记录"节（追加），不提交单独 commit（随 Phase 1 首个 commit 或最后统一记录）。

---

## Phase 1：B 组 · 数据与过滤（3 / 4 / 10）

### Task 1: 会话编号显示序号化（3️⃣）

**Files:**
- Modify: `app/src-tauri/src/db_sessions.rs`（list_sessions 返回 display_no）
- Modify: `app/src-tauri/src/commands_session.rs`（会话列表 command 组装）
- Modify: `app/src-tauri/src/db_sessions_tests.rs`（新增测试）
- Modify: `app/src/components/SessionListPanel.tsx`（显示 display_no）
- Modify: `app/src/types/session.ts`（SessionListItem 增 display_no）

- [ ] **Step 1: 写失败测试（rank 计算）**

在 `db_sessions_tests.rs` 新增：
```rust
#[test]
fn display_no_is_created_order_rank() {
    // Arrange：三个会话（乱序创建时间），删除中间一个
    let db = mem_db();
    let a = db.create_session(&new_session("最早")).unwrap();
    let b = db.create_session(&new_session("中间")).unwrap();
    let c = db.create_session(&new_session("最晚")).unwrap();
    db.delete_session(b.id).unwrap();
    // Act
    let list = db.list_sessions(None, 10, 0).unwrap();
    // Assert：rank 按 started_at 升序 1..=n（删除后重排，不复用 b 的序号）
    let ranks: Vec<i64> = list.iter().map(|s| s.display_no).collect();
    assert_eq!(ranks, vec![1, 2]);
    assert_eq!(list[0].session.id, a.id);
    assert_eq!(list[1].session.id, c.id);
}
```
说明：`Session` 结构体**不加字段**（内部 id 永不变）；在列表查询结果（`SessionListItem` 或新返回结构）中携带 display_no。若 `list_sessions` 现返回 `Vec<Session>`，则在 `Session` 派生展示结构或命令层组装——**推荐命令层组装**：`commands_session.rs` 的 list 命令查询后按 `(started_at, id)` 升序赋 rank。

- [ ] **Step 2: 运行测试确认失败**

运行：`cd "d:\Program own\aicode\work space\Entropydecrease\app\src-tauri"; cargo test display_no_is_created_order_rank`
预期：编译失败（display_no 字段不存在）。

- [ ] **Step 3: 实现 rank 组装**

`commands_session.rs` 列表命令中，在返回列表前统一赋 rank（列表与课程分组两个数据源**合并后**赋 rank，保证全局连续）：
```rust
// 伪代码：合并 items（session + groups.sessions 去重）→ 按 (started_at, id) 排序 → 赋 display_no
let mut all: Vec<&Session> = /* 合并去重 */;
all.sort_by(|a, b| a.started_at.cmp(&b.started_at).then(a.id.cmp(&b.id)));
let map: HashMap<i64, i64> = all.iter().enumerate().map(|(i, s)| (s.id, i as i64 + 1)).collect();
// 每条 SessionListItem 填 display_no = map[&s.id]
```
- 前端 `SessionListPanel.tsx` 第 248 行 `#{s.id}` → `#{item.display_no}`（`SessionListItem` 增字段 `display_no: number`，类型文件 `app/src/types/session.ts` 同步）
- 详情面板/笔记跳转等**仍用内部 id**（不显示编号处不改）

- [ ] **Step 4: 运行测试确认通过**

运行：`cd "d:\Program own\aicode\work space\Entropydecrease\app\src-tauri"; cargo test display_no`
预期：PASS。前端 `cd app; npx tsc --noEmit` 通过。

- [ ] **Step 5: 提交**

```bash
git add app/src-tauri/src/commands_session.rs app/src-tauri/src/db_sessions_tests.rs app/src/components/SessionListPanel.tsx app/src/types/session.ts
git commit -m "feat(session): 会话列表展示序号与内部 id 分离（删除后归位显示）"
```

### Task 2: 新颖度比较域=变化区域（4️⃣）

**Files:**
- Modify: `app/src-tauri/src/novelty.rs`（阈值参数化 + 变化区域输入语义）
- Modify: `app/src-tauri/src/live_keyframes.rs`（调用处传"变化区域文本"）
- Modify: `app/src-tauri/src/import_frame.rs`（导入链路同步——same_texts 保持，novelty 语义不引入导入链路，**本任务只改实时链路**）
- Modify: `app/src-tauri/src/novelty_tests.rs`（新增黄金用例）
- Modify: `app/src-tauri/src/live_frame_process.rs`（组装变化区域文本——若 OCR 块已带 bbox，按 grid_diff 变化格反算筛选）

- [ ] **Step 1: 写失败测试（PPT 同构页黄金用例）**

`novelty_tests.rs` 新增：
```rust
#[test]
fn template_page_same_structure_different_content_not_redundant() {
    // Arrange：PPT 模板同构——页脚/侧栏固定，正文不同（模拟"变化区域文本"输入：
    // 调用方只传变化区域文本，固定版面文字不参与）
    let recent = vec![t("页脚·课程名称"), t("第一章 变量定义")];
    let new = vec![t("页脚·课程名称"), t("第二章 函数调用")];
    // Act：仅正文比较（调用方已过滤固定文本——此处直接传正文）
    let score = novelty_score(&[t("第二章 函数调用")], &[t("第一章 变量定义")]);
    // Assert：全新正文 → 低重叠 → 非冗余
    assert!(score < REDUNDANT_THRESHOLD, "模板同构正文不同不得判冗余，实得 {}", score);
    // 旧语义回归：整帧含固定文本时高重叠（证明"变化区域"是调用方责任）
    let full_score = novelty_score(&new, &recent);
    assert!(full_score >= REDUNDANT_THRESHOLD);
}

#[test]
fn threshold_adapts_by_tier() {
    // Arrange：画面档自适应阈值
    // Act & Assert：rich 档 0.90 更宽松——0.87 判非冗余；low 档 0.80 更严格——0.82 判冗余
    assert!(!is_redundant(0.87, tier_threshold("rich")));
    assert!(is_redundant(0.82, tier_threshold("low")));
    assert_eq!(tier_threshold("rich"), 0.90);
    assert_eq!(tier_threshold("low"), 0.80);
    assert_eq!(tier_threshold("medium"), 0.85);
}
```
`tier_threshold` 为新公开函数（`pub fn tier_threshold(tier: &str) -> f32`，未知档回退 0.85）。

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test template_page_same_structure_different_content_not_redundant`
预期：PASS（该用例只验证 novelty_score 语义，应本就通过——此用例是**契约钉死**）；`cargo test threshold_adapts_by_tier` 预期 FAIL（函数不存在）。

- [ ] **Step 3: 实现**

`novelty.rs` 新增：
```rust
/// 画面档 → 冗余阈值（rich 更宽松——画面价值高时少判冗余；low 更严格）。
pub fn tier_threshold(tier: &str) -> f32 {
    match tier {
        "rich" => 0.90,
        "low" => 0.80,
        _ => REDUNDANT_THRESHOLD,
    }
}
```
`live_keyframes.rs` 第 64-69 行调用处改为：传入**变化区域文本**（`live_frame_process.rs` 在调用 collect 前，用 grid_diff 的 changed_cells → bbox 反算 → 仅保留落在变化格内的 OCR 块文本；变化格为空则回退整帧文本）+ 阈值用 `tier_threshold(tier)`（tier 由调用方传入，未知传 "medium"）。
注意 `live_keyframes.rs` 的 `collect_keyframes` 签名可能需增加 `tier: &str` 参数（或复用已有参数）；`live_frame_process.rs` 调用处同步。

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test novelty` 全绿；`cargo test live_keyframes` 回归通过。

- [ ] **Step 5: 提交**

```bash
git add app/src-tauri/src/novelty.rs app/src-tauri/src/novelty_tests.rs app/src-tauri/src/live_keyframes.rs app/src-tauri/src/live_frame_process.rs
git commit -m "feat(filter): 新颖度比较域改为变化区域 + 阈值按画面档自适应（PPT 同构页不再误过滤）"
```

### Task 3: 水印过滤 A+B+C（10️⃣）

**Files:**
- Modify: `app/src-tauri/src/watermark_filter.rs`（B 文本相似聚类 + C 区域级出现率；文件超 300 行则拆 `watermark_similar.rs`）
- Modify: `app/src-tauri/src/screens.rs`（A：bbox → region_key 接入）
- Modify: `app/src-tauri/src/analysis.rs`（A：同源接入）
- Modify: `app/src-tauri/src/watermark_filter_tests.rs`（会话 38 回归 + 聚类用例）

- [ ] **Step 1: 写失败测试（会话 38 水印 + 聚类 + 出现率）**

`watermark_filter_tests.rs` 新增：
```rust
#[test]
fn ocr_jitter_variants_clustered_as_one_watermark() {
    // Arrange：半透明水印 OCR 抖动——同区域同位置文本每帧略有差异
    let mut blocks = Vec::new();
    for i in 0..10 {
        let variant = match i % 3 {
            0 => "万事如番茄LilLil",
            1 => "万事如番茄LilLil ",   // 尾随空格（OCR 抖动）
            2 => "万事如番茄LilL",     // 截断变体
            _ => unreachable!(),
        };
        blocks.push(wm(variant, i * 10_000, "corner-br"));
        blocks.push(wm(&format!("正文内容{}", i), i * 10_000, "slide"));
    }
    // Act：启用相似聚类的检测入口
    let result = detect_watermarks_similar(&blocks, &WatermarkConfig::default());
    // Assert：抖动变体归并为一个候选 → 计数 10 帧 ≥ 5 → 命中
    assert!(result.texts.iter().any(|t| t.contains("万事如番茄LilLil")));
}

#[test]
fn region_appearance_rate_detects_stable_text() {
    // Arrange：区域稳定 + 文本近似不变（具体文本与阈值下计数不足，但区域出现率高）
    let mut blocks = Vec::new();
    for i in 0..7 {
        // 同一区域出现 7 帧，但文本每次略变（抖动严重，相似聚类也难归并 → 走区域出现率）
        blocks.push(wm(&format!("角标{}", i), i * 10_000, "corner-br"));
    }
    // Act：区域级出现率检测（C 层）
    let result = detect_region_watermarks(&blocks, &WatermarkConfig::default());
    // Assert：区域 corneer-br 出现率 7/7、区域内文本变化但区域稳定 → 命中区域水印
    assert!(result.texts.is_empty() || !result.texts.is_empty()); // 断言随实现定稿：见 Step 3 说明
}
```
> 实现说明（定稿语义）：`detect_region_watermarks` 按 `(region_key)` 聚合——出现帧数 ≥ min_occurrences 且**区域内文本变化但内容帧在变**（整帧签名种类 ≥ min_distinct_frames）→ 区域水印（texts 输出**众数文本**或空则区域键）；`detect_watermarks_similar` = 现 detect_watermarks + 编辑距离 ≤2 归并。两函数共用内部统计核心。测试断言在实现时按定稿语义精确化（Step 2 先跑通骨架）。

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test ocr_jitter_variants_clustered_as_one_watermark`
预期：编译失败（函数不存在）。

- [ ] **Step 3: 实现**

`watermark_filter.rs`：
- 重构内部为 `struct Acc`（现结构）+ 新增两个公开入口：
```rust
/// 相似聚类水印检测：编辑距离 ≤2 的文本归并同一候选（OCR 抖动防御）。
pub fn detect_watermarks_similar(blocks: &[WatermarkInput], cfg: &WatermarkConfig) -> WatermarkResult
/// 区域出现率检测：区域稳定 + 区域内文本变化 + 整帧内容在变 → 区域级水印。
pub fn detect_region_watermarks(blocks: &[WatermarkInput], cfg: &WatermarkConfig) -> WatermarkResult
```
- 编辑距离 ≤2：朴素 DP（文本短，O(n*m) 可接受）；归并策略——贪心：按出现次数降序，新文本与已归并代表文本距离 ≤2 → 并入（代表=出现最多的原文）
- `detect_watermarks`（旧入口）保持签名不变（兼容既有调用），内部委托相似聚类版（行为兼容：无抖动时等价）
- `screens.rs` 第 73 行附近：`region_key: None` → 从 `b.bbox`（DB 已落库）构造区域键（归一化网格：`bbox` 有值则按坐标 4x4 网格单元键如 "g1-3"；无 bbox 保持 None）；`analysis.rs` 同源
- 区域出现率的"内容在变"证据：沿用整帧签名种类 ≥ min_distinct_frames（现逻辑已有）

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test watermark` 全绿（旧用例回归 + 新用例）；`cargo test screens` 回归通过。

- [ ] **Step 5: 提交**

```bash
git add app/src-tauri/src/watermark_filter.rs app/src-tauri/src/watermark_filter_tests.rs app/src-tauri/src/screens.rs app/src-tauri/src/analysis.rs
git commit -m "feat(filter): 水印过滤 bbox 接入 + 文本相似聚类 + 区域出现率（会话38水印回归）"
```

---

## Phase 2：A 组 · 采集体验（11 / 1 / 2）

### Task 4: 窗口选择展开自动刷新（11️⃣）

**Files:**
- Modify: `app/src/components/WindowSelectCard.tsx`（展开触发刷新）
- Modify: `app/src/pages/ClassroomPage.tsx`（refreshWindows 后台模式）

- [ ] **Step 1: 前端改造**

`WindowSelectCard.tsx`：浮层打开时（首次点击卡片展开）调用新 prop `onOpenRefresh`（或复用 onRefresh——但需"后台模式"不闪空）。`ClassroomPage.tsx` 的 `refreshWindows` 增加后台模式：
```ts
const refreshWindows = useCallback(async (background = false) => {
  if (!background) setWindowsLoading(true);
  try {
    const list = await invoke<WindowInfo[]>("list_windows");
    setWindows(list);
  } catch (e) {
    setStatus(`窗口枚举失败: ${e}`);
  } finally {
    if (!background) setWindowsLoading(false);
  }
}, []);
```
展开时调 `refreshWindows(true)`（不置 loading、不清列表，完成后替换）；手动按钮保持 `refreshWindows()`。

- [ ] **Step 2: 验证**

运行：`cd app; npx tsc --noEmit` 通过；`npm run build` 通过。手工：展开浮层→新开一个窗口→列表出现新窗口且无闪烁。

- [ ] **Step 3: 提交**

```bash
git add app/src/components/WindowSelectCard.tsx app/src/pages/ClassroomPage.tsx
git commit -m "feat(classroom): 窗口选择浮层展开时自动后台刷新列表"
```

### Task 5: 记忆机制四象限（1️⃣ 的一部分）

**Files:**
- Modify: `app/src-tauri/src/commands_video.rs`（记忆后置兜底 + 冲突判定）
- Modify: `app/src-tauri/src/video_profile.rs`（vote_detect 输出 top kind）
- Modify: `app/src-tauri/src/video_profile_tests.rs` 或新建 `commands_video_tests.rs`（四象限矩阵）

- [ ] **Step 1: 写失败测试（四象限）**

新建 `app/src-tauri/src/commands_video_tests.rs`（若 commands_video.rs 已有测试文件则追加）。测试通过注入 fake `ProfileMemory`（查现有 ProfileMemory 结构/接口——`memory.lookup(t)` 返回 `Option<ProfileKind>`，`lookup_form` 返回 form；构造内存实例或 mock）：
```rust
#[test]
fn memory_high_conf_same_kind_wins() { /* 象限①：记忆 Lecture + 检测高置信 Lecture → memory_hit=Some(Lecture), needs_confirmation=false */ }
#[test]
fn memory_high_conf_conflict_detection_wins() {
    // 象限②：记忆 HandsOn + 检测高置信 Lecture（帧切换率高/字幕命中）→ candidates 首位=Lecture，
    // memory_hit=None（或标记冲突字段），needs_confirmation=false，且结果带冲突提示
}
#[test]
fn memory_low_conf_fallback_to_memory() {
    // 象限③：记忆 Lecture + 检测低置信（信号不足/冲突）→ memory_hit=Some(Lecture)，无需确认
}
#[test]
fn no_memory_pure_detection() { /* 象限④：无记忆 → 现状检测结果 */ }
```
> 冲突提示字段：`DetectResult` 新增 `#[serde(default)] pub memory_conflict: Option<ProfileKind>`（记忆 kind；前端展示"记忆与新检测冲突，已按检测"）。

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test memory_high_conf_conflict_detection_wins`
预期：FAIL（现状记忆前置短路——象限②必失败）。

- [ ] **Step 3: 实现**

`commands_video.rs` `detect_video_profile` 重构：
```rust
// 1) 先跑信号投票（vote_detect 保持）
let mut result = vote_detect(&ObservedSignals { ... });
// 2) 记忆后置兜底 + 冲突判定
if let Some(t) = title.as_deref() {
    let memory = state.profile_memory.lock().map(|m| m.clone()).unwrap_or_default();
    if let Some(kind) = memory.lookup(t) {
        let top = result.candidates.first().map(|c| c.kind);
        if !result.needs_confirmation && top.is_some() && top != Some(kind) {
            // 象限②：检测高置信且冲突 → 检测为准 + 标记冲突（不自动改记忆）
            result.memory_conflict = Some(kind);
        } else if result.needs_confirmation || top == Some(kind) {
            // 象限①/③：同 kind 或检测低置信 → 记忆生效（快路径）
            result.candidates = vec![ProfileCandidate { kind, score: 1.0 }];
            result.needs_confirmation = false;
            result.memory_hit = Some(kind);
            result.memory_form = memory.lookup_form(t);
        }
    }
}
```
`video_profile.rs`：`DetectResult` 增 `memory_conflict` 字段（serde default 保持兼容）。注意：原逻辑在投票**前**查记忆并短路返回，重构后投票始终执行（检测信号消费统一）。

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test`（四象限 + video_profile 既有全量回归）。前端 ProfileDetector 显示"记忆与新检测冲突，已按检测"（改 `ProfileDetector.tsx` 读 `memory_conflict` 字段）。

- [ ] **Step 5: 提交**

```bash
git add app/src-tauri/src/commands_video.rs app/src-tauri/src/video_profile.rs app/src-tauri/src/commands_video_tests.rs app/src/components/ProfileDetector.tsx
git commit -m "feat(profile): 记忆机制改检测优先+记忆兜底+冲突以检测为准（四象限）"
```

### Task 6: 采集中档案热切换 + 自动重评（1️⃣ 主体）

**Files:**
- Modify: `app/src-tauri/src/live_session_lifecycle.rs` 或 `live_session_manager.rs`（新命令 + 重评 tick）
- Modify: `app/src-tauri/src/commands_live.rs`（`update_live_profile` command）
- Modify: `app/src-tauri/src/lib.rs`（命令注册）
- Modify: `app/src/components/LiveProfileStrip.tsx`（三下拉 + 调命令 + 监听事件）
- Modify: `app/src-tauri/src/live_*.rs`（重评 tick 扩展——定位现有 tier 重评代码，同节奏加形态/领域重评）

- [ ] **Step 1: 后端命令 `update_live_profile`**

`commands_live.rs` 新增（TDD：先写失败测试——命令入参校验）：
```rust
/// 采集态档案热切换（v0.11.5）：只影响后续采集，不重算历史。
#[tauri::command]
pub fn update_live_profile(
    state: State<'_, AppState>,
    form: Option<String>,   // kebab-case ContentForm
    tier: Option<String>,   // kebab-case VisualTier
    domain: Option<String>, // kebab-case DomainKind
) -> Result<bool, String> {
    // 校验：form/tier/domain 任选其一或组合；非法值拒绝（白名单 parse）
    // 生效：写入 live session 共享 override（与 tier override 同通道）→ 管线下次采样 tick 消费
    // 事件：app.emit("live:profile-updated", payload)
}
```
测试：无效 form 拒绝 / 有效组合通过 / 无活动会话报错。先写测试跑失败 → 实现 → 跑通。

- [ ] **Step 2: 重评 tick 扩展**

定位现有画面档重评代码（tier 每 2-3 分钟重评——搜 `tier` + `reevaluate`/`resample` 相关 tick 函数），同 tick 追加：
- 形态/领域重评：调用 `detect_video_profile`（带增量信号：当前帧切换率统计/字幕计数/新 OCR 分区标签——Task 7 的信号源接入后可用）
- 重评结果若与当前生效档案不同且高置信 → `live:profile-updated` 事件 + 自动热切换（或低置信仅提示——按 spec：检测高置信自动生效，低置信不打扰）
- 更新 `LiveProfileStrip.tsx`：三下拉（复用 ProfileDetector 的 FORM_LABELS/TIER_LABELS/DOMAIN_OPTIONS + changeXxx 逻辑）→ 调 `update_live_profile`；监听 `live:profile-updated` 刷新展示

- [ ] **Step 3: 验证**

运行：`cargo test` 全量回归（新增命令测试通过）；前端 `npx tsc --noEmit` 通过。
手工验收：采集开始 → 改形态为"实操" → 观察采样节奏变化（信息面板/状态行）→ 改回。

- [ ] **Step 4: 提交**

```bash
git add app/src-tauri/src/commands_live.rs app/src-tauri/src/live_session_*.rs app/src-tauri/src/lib.rs app/src/components/LiveProfileStrip.tsx
git commit -m "feat(classroom): 采集态档案三维热切换 + 形态/领域自动重评（同画面档节奏）"
```

### Task 7: 视频信息识别率增强（2️⃣）

**Files:**
- Modify: `app/src-tauri/src/video_profile_domain.rs`（DomainSignals 增 asr_opening）
- Modify: `app/src-tauri/src/video_profile_domain_tests.rs`（ASR 开场白 golden）
- Modify: `app/src-tauri/src/platform_adapter.rs`（B站选集 OCR 解析）
- Modify: `app/src-tauri/src/platform_adapter_tests.rs`（B站样本矩阵）
- Modify: `app/src-tauri/src/commands_video.rs`（asr_opening 参数接线）或 live 重评 tick 传入

- [ ] **Step 1: 写失败测试（ASR 开场白 → 领域）**

`video_profile_domain_tests.rs` 新增：
```rust
#[test]
fn asr_opening_intro_detects_domain() {
    // Arrange：会话开头 30s ASR 文本（开场白自我介绍）
    let signals = DomainSignals {
        title: "零基础教程".to_string(),
        platform_tags: vec![],
        user_confirmed: None,
        term_freq: vec![],
        asr_opening: Some("大家好我是美妆博主，今天教大家画眼影".to_string()),
    };
    // Act
    let d = detect_domain(&signals);
    // Assert：开场白领域词命中 → beauty
    assert_eq!(d.kind.as_deref(), Some("beauty"));
}
```
`DomainSignals` 增 `#[serde(default)] asr_opening: Option<String>`（向后兼容）；`detect_domain` 增加开场白关键词扫描（领域词表扩展：美妆/编程/乐理等现有 DOMAIN 表 + 开场白口语模式"大家好/我是/今天教/教你/分享"后的领域词）。

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test asr_opening_intro_detects_domain` 预期 FAIL（字段不存在）。

- [ ] **Step 3: 实现 + B站适配试点**

- `video_profile_domain.rs`：asr_opening 字段 + 扫描逻辑（复用现有领域关键词表）
- `platform_adapter.rs` 新增：
```rust
/// B站选集按钮 OCR 文本 → 集号/总集数（"P3/12"、"第3集/共12集"形态——与 session_info parse_player_text 同解析，此处复用它）
pub fn adapt_bilibili_episode(ocr_text: &str) -> Option<(u32, Option<u32>)>
```
（实现：复用/委托 `session_info.rs` 的 `parse_player_text`——避免双份解析逻辑漂移；若接口不匹配则内部调用）
- 接线：live 重评 tick 或 `commands_video.rs` 增加可选参数 `player_ocr_text: Option<String>` → B站平台时解析选集 → 合集信息（episode/total）汇入检测信号与面板信息

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test video_profile_domain platform_adapter` 全绿；全量 `cargo test` 回归。

- [ ] **Step 5: 提交**

```bash
git add app/src-tauri/src/video_profile_domain.rs app/src-tauri/src/video_profile_domain_tests.rs app/src-tauri/src/platform_adapter.rs app/src-tauri/src/platform_adapter_tests.rs app/src-tauri/src/commands_video.rs
git commit -m "feat(profile): 领域检测增 ASR 开场白信号 + B站选集 OCR 适配试点"
```

---

## Phase 3：C 组 · 会话页（7 / 8 / 5 / 6）

### Task 8: AI 输入剥离时间戳锚点（7️⃣）

**Files:**
- 新建: `app/src-tauri/src/anchor_strip.rs`（纯函数：剥离/回挂——保持模块 ≤300 行）+ `anchor_strip_tests.rs`
- Modify: `app/src-tauri/src/ai_refine_task.rs`（输入预处理）
- Modify: `app/src-tauri/src/ai_refine_protocol.rs`（章节锚点回挂）
- Modify: `app/src-tauri/src/commands_ai_refine.rs`（预估字符数修正）

- [ ] **Step 1: 写失败测试（纯函数）**

新建 `anchor_strip.rs`：
```rust
/// 剥离全部时间戳锚点：`[⏱ MM:SS]([[ts:ms]])` → 空串；章节标题锚点形态
/// `## 标题 [[⏱ ...]](...)` 保留标题、记录锚点（回挂用）。
pub fn strip_anchors(markdown: &str) -> String
/// 剥离并收集章节锚点：返回 (剥离后文本, [(章节标题, ts)])。
pub fn strip_anchors_with_map(markdown: &str) -> (String, Vec<(String, u64)>)
/// 章节锚点回挂：精修输出中匹配的章节标题后挂回锚点。
pub fn reattach_chapter_anchors(markdown: &str, anchors: &[(String, u64)]) -> String
```
测试用例（`anchor_strip_tests.rs`）：
```rust
#[test]
fn strip_removes_paragraph_anchors() {
    let md = "[⏱ 00:00]([[ts:0]]) 项目启动是一个艺术。\n\n[⏱ 01:03]([[ts:63000]]) 这是项目章程。";
    let out = strip_anchors(md);
    assert_eq!(out, "项目启动是一个艺术。\n\n这是项目章程。");
}
#[test]
fn strip_keeps_chapter_title_and_records_anchor() {
    let md = "## 项目章程 [[⏱ 00:09]([[ts:9000]])]";
    let (out, anchors) = strip_anchors_with_map(md);
    assert_eq!(out, "## 项目章程");
    assert_eq!(anchors, vec![("项目章程".to_string(), 9000)]);
}
#[test]
fn reattach_restores_chapter_anchors() {
    let anchors = vec![("项目章程".to_string(), 9000)];
    let out = reattach_chapter_anchors("## 项目章程\n正文", &anchors);
    assert_eq!(out, "## 项目章程 [[⏱ 00:09]([[ts:9000]])]\n正文");
}
```
（锚点匹配用正则——项目已用 regex crate 则直接用；未用则手写解析：先找 `[[ts:数字]]`，再向前回溯 `[⏱` 与 `](`）

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test anchor_strip` 预期：编译失败（文件不存在）。

- [ ] **Step 3: 实现**

- `anchor_strip.rs`：实现三函数
- `ai_refine_task.rs`：任务输入 = `build_rule_draft` 输出 → 先 `strip_anchors_with_map` → 剥离后 markdown 送切片/精修；anchors 随任务结果回传
- `ai_refine_protocol.rs`：精修输出合并后执行 `reattach_chapter_anchors`（章节标题匹配 anchors；未匹配不挂——诚实降级）；规则版落库路径不动（段落锚点保留）
- `commands_ai_refine.rs`：`ai_refine_estimate` 字符数 = `strip_anchors(draft).chars().count()`

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test anchor_strip ai_refine` 全绿；在 ai_refine_task 测试中加断言：精修请求序列化后不含 `[[ts:`。

- [ ] **Step 5: 提交**

```bash
git add app/src-tauri/src/anchor_strip.rs app/src-tauri/src/anchor_strip_tests.rs app/src-tauri/src/ai_refine_task.rs app/src-tauri/src/ai_refine_protocol.rs app/src-tauri/src/commands_ai_refine.rs
git commit -m "perf(ai): AI 精修输入剥离时间戳锚点 + 章节级回挂 + 预估修正"
```

### Task 9: 画面要点/词汇表移出笔记 + 详情术语表（8️⃣）

**Files:**
- Modify: `app/src-tauri/src/note_filter.rs`（移除 ocr_points 段/glossary 输出）
- Modify: `app/src-tauri/src/concat.rs`（assemble_markdown 去 ocr_points）
- Modify: `app/src-tauri/src/structure_note.rs`（词汇表段输出移除）
- Modify: `app/src-tauri/src/commands_session.rs` 或新命令（详情术语表 `session_glossary`）
- Modify: `app/src/components/NotePreviewView.tsx`（同步）
- Modify: `app/src/components/SessionDetailPanel.tsx`（原料视图术语表折叠区）
- Modify: `app/src-tauri/src/note_filter_golden_tests.rs` 及所有依赖画面要点断言的测试

- [ ] **Step 1: 写失败测试（术语表命令）+ 更新 golden**

新建命令测试：
```rust
#[test]
fn session_glossary_returns_terms() {
    // Arrange：会话含 OCR 高频术语（分析层 glossary 产出路径）
    // Act：调用 session_glossary(id)
    // Assert：返回术语列表（term + score + 出现来源）
}
```
`note_filter_golden_tests.rs`：把"画面要点"相关断言改为反向断言：
```rust
#[test]
fn note_excludes_ocr_points_and_glossary() {
    let result = run("测试", &segments, &blocks);
    assert!(!result.markdown.contains("## 画面要点"), "画面要点不得进笔记");
    assert!(!result.markdown.contains("术语"), "词汇表不得进笔记");
}
```

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test session_glossary note_excludes_ocr_points` 预期：FAIL（命令不存在/反向断言失败）。

- [ ] **Step 3: 实现**

- `note_filter.rs`：过滤链输出不再拼"画面要点"段；词汇表不进笔记 markdown（`structure_note.rs` 的 glossary 段删除）
- `concat.rs`：`assemble_markdown` 签名保留（兼容），调用处不传 OCR 点（旧文件导入链路同步）
- 详情术语表命令：
```rust
/// 会话术语表（v0.11.5）：分析层 glossary 数据展示（会话内可见，不进笔记）。
#[tauri::command]
pub async fn session_glossary(state: State<'_, AppState>, id: i64) -> Result<Vec<GlossaryTerm>, String>
```
`GlossaryTerm { term: String, score: f32, ocr_count: u32, asr_count: u32 }`（复用 analysis.rs glossary 结构或定义展示结构）
- `SessionDetailPanel.tsx` 原料视图新增"术语表"折叠区（调 session_glossary）

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test` 全量（含更新后的 golden）通过；前端 `npx tsc --noEmit` 通过。
**注意**：既有 golden 测试大量断言画面要点/配图行——逐一更新为"不含"断言或删除对应断言；配图行能力由 AI 精修 image 块承载（F3 v2 已支持），note_filter 的配图行输出同步移除。

- [ ] **Step 5: 提交**

```bash
git add app/src-tauri/src/note_filter.rs app/src-tauri/src/concat.rs app/src-tauri/src/structure_note.rs app/src-tauri/src/commands_session.rs app/src-tauri/src/note_filter_golden_tests.rs app/src/components/NotePreviewView.tsx app/src/components/SessionDetailPanel.tsx
git commit -m "feat(note): 画面要点/词汇表移出笔记（会话详情术语表展示）"
```

### Task 10: 产物视图下线 + 课后精修懒自动（5️⃣）

**Files:**
- Modify: `app/src/components/SessionDetailPanel.tsx`（三视图→两视图；原料视图自动触发）
- Modify: `app/src-tauri/src/commands_refine.rs`（自动精修触发 + 幂等检查）
- Modify: `app/src-tauri/src/commands_refine_inner.rs`（精修幂等：已精修屏跳过）
- Modify: `app/src-tauri/src/screens.rs`（结构区域精修状态标记）
- Delete: `app/src/components/ArtifactView.tsx`（组件删除；命令保留）

- [ ] **Step 1: 写失败测试（精修幂等）**

`commands_refine` 相关测试文件新增：
```rust
#[test]
fn refine_skips_already_refined_screens() {
    // Arrange：会话含 2 个结构屏，1 个已精修（状态标记存在）
    // Act：触发自动精修
    // Assert：仅处理未精修屏；已精修屏跳过（返回 skipped 明细）
}
```

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test refine_skips_already_refined_screens` 预期 FAIL。

- [ ] **Step 3: 实现**

- `screens.rs`：结构区域加精修状态标记（检查 `refine_screen_structures` 现状——精修后结构块已更新；幂等标记 = 结构块已含模型版内容或新增 `refined_at` 字段 serde default）
- `commands_refine.rs` 新增自动触发命令（或复用 refine_session 加幂等前置）：
```rust
/// 自动精修（懒触发）：仅处理未精修结构区域；无待精修屏快速返回（零成本）。
#[tauri::command]
pub async fn auto_refine_session(state: State<'_, AppState>, session_id: i64) -> Result<String, String>
```
内部：查未精修结构区域 → 空则返回 "no-pending"；非空走现有 run_refine（含模型缺失降级）
- `SessionDetailPanel.tsx`：视图切换去"产物"；进入原料视图（viewMode==="raw"）时调 `auto_refine_session`（防抖/仅首次进入触发）；监听 `session:refined` 刷新屏卡（现有逻辑在 ArtifactView——迁移到面板层）
- `ArtifactView.tsx` 删除（`git rm`）

- [ ] **Step 4: 运行测试确认通过**

运行：`cargo test refine` 全绿；前端 tsc/vite 通过。手工：进入详情（原料视图）→ 结构屏自动精修 → 屏卡徽标/内容回填；二次进入不重复精修。

- [ ] **Step 5: 提交**

```bash
git add app/src/components/SessionDetailPanel.tsx app/src-tauri/src/commands_refine.rs app/src-tauri/src/commands_refine_inner.rs app/src-tauri/src/screens.rs
git rm app/src/components/ArtifactView.tsx
git commit -m "feat(session): 产物视图下线 + 课后精修懒自动化（原料视图自动触发）"
```

### Task 11: 精修工作台（6️⃣）

**Files:**
- Modify: `app/src-tauri/src/note_diff.rs`（章节级 hunk 分组输出）
- 新建: `app/src-tauri/src/note_diff_tests.rs`（若不存在）
- Modify: `app/src-tauri/src/commands_ai_refine.rs`（工作台查询接口）
- 新建: `app/src/components/RefineWorkbench.tsx`（工作台模态）
- Modify: `app/src/components/AiRefineCard.tsx`（入口 → 打开工作台）
- Modify: `app/src/components/VersionPanel.tsx`（"与当前版本对比"）
- Modify: `app/src/components/NotePreviewView.tsx`（已采纳版本展示——6️⃣ 配套）

- [ ] **Step 1: 写失败测试（diff 章节分组纯函数）**

`note_diff.rs` 新增：
```rust
/// 章节级分组 diff：行级 diff 之上按章节标题聚合为 hunk（一个逻辑改动=一个 hunk）。
pub fn diff_sections(old: &str, new: &str) -> Vec<SectionDiff>
pub struct SectionDiff {
    pub heading: String,          // 章节标题（无标题段用 "（无标题）"）
    pub status: DiffStatus,       // Modified/Added/Removed/Unchanged
    pub removed_lines: Vec<String>, // 删除明细（折叠可展开）
    pub added_lines: Vec<String>,
}
```
测试：
```rust
#[test]
fn section_rename_groups_as_one_hunk() {
    // 章节变更 → 一个 hunk 而非逐行噪声
    let old = "## 第一章\nA\nB\n## 第二章\nC";
    let new = "## 第一章\nA\nB\n## 第三章\nD";
    let d = diff_sections(old, new);
    assert!(d.iter().any(|s| s.status == Modified && s.heading.contains("第三章")));
    assert!(d.iter().any(|s| s.status == Removed && s.heading.contains("第二章")));
}
#[test]
fn removed_lines_expandable() {
    let old = "## 章\n甲\n乙";
    let new = "## 章\n甲";
    let d = diff_sections(old, new);
    let rm = d.iter().find(|s| s.heading == "章").unwrap();
    assert_eq!(rm.removed_lines, vec!["乙".to_string()]);
}
```

- [ ] **Step 2: 运行测试确认失败**

运行：`cargo test diff_sections` 预期：编译失败。

- [ ] **Step 3: 实现（后端）**

- `note_diff.rs`：`diff_sections`——按 `^#{1,6} ` 切章节块 → 块级 LCS 比较（复用现有行级算法在块粒度聚合）；无标题内容归"（无标题）"
- `commands_ai_refine.rs` 新增工作台查询：
```rust
/// 精修工作台数据（一次取全：规则草稿 + 精修结果 + 章节分组 diff）。
#[tauri::command]
pub async fn refine_workbench(state: State<'_, AppState>, session_id: i64) -> Result<WorkbenchData, String>
pub struct WorkbenchData {
    pub rule_markdown: String,     // 剥离锚点后的规则版（展示用）
    pub refined_markdown: Option<String>, // 精修版（无则 None）
    pub sections: Vec<SectionDiff>, // 章节分组 diff
    pub stats: DiffStats,          // 新增/删除行统计（复用现有 diff_stats）
    pub meta: Option<VersionMeta>, // 成本/模型/切片
}
```

- [ ] **Step 4: 实现（前端 RefineWorkbench.tsx）**

组件结构（≤300 行，状态机 idle/running/done/failed）：
```
- Props: sessionId, onClose
- 顶部：统计条（新增 N/删除 M/切片 K/成本 ¥X）+ 关闭
- 主体：双栏（左 rule_markdown / 右 refined_markdown）
  - 同步滚动：左栏 onScroll → 右栏 scrollTop 镜像（比例换算）
  - 章节 hunk：右侧按 SectionDiff 渲染——modified 章节高亮底纹（新增绿/删除红折叠条"已删除 X 段 ▸"）
  - 章节锚点：两栏标题渲染 [[ts:]] 回跳（复用 NoteMarkdown 的 a 渲染逻辑）
- 底部：⟳ 重新生成（调 ai_refine_start）│ 放弃（onClose）│ ✅ 采纳落库（ai_refine_apply → 版本化 → onApplied）
```
- `AiRefineCard.tsx`：任务完成后主按钮改"打开工作台"（内嵌小 diff 移除）；成本确认/授权流程保留
- `VersionPanel.tsx`：每版本行加"对比"按钮 → 复用 diff_sections + 双栏渲染（工作台只读模式或简化内嵌对比，取实现量小者）
- `NotePreviewView.tsx`（6️⃣ 配套——精修回退根治）：挂载时查询该会话最新笔记（数据层 `find_note_by_session` 已有——补 command `note_by_session` 若无）：存在 → 展示已落库版本（精修版）+"重新生成规则版预览"入口；不存在 → 现状规则草稿。切页回来不再回退（数据源持久化）

- [ ] **Step 5: 验证**

运行：`cargo test` 全绿（diff_sections + workbench 查询）；前端 tsc/vite/vitest（工作台状态机测试：mock invoke 断言运行中→完成→采纳流转）。
手工验收：精修完成 → 打开工作台 → 双栏同步滚动 → 章节 hunk 高亮/删除展开 → 采纳落库 → 版本面板可见新版本 → 切页回来预览显示精修版。

- [ ] **Step 6: 提交（两个 commit）**

```bash
git add app/src-tauri/src/note_diff.rs app/src-tauri/src/note_diff_tests.rs app/src-tauri/src/commands_ai_refine.rs
git commit -m "feat(ai): 精修工作台后端——章节分组 diff + 工作台数据接口"

git add app/src/components/RefineWorkbench.tsx app/src/components/AiRefineCard.tsx app/src/components/VersionPanel.tsx app/src/components/NotePreviewView.tsx
git commit -m "feat(ai): 精修工作台 UI（并排双栏+语义 diff+采纳落库）+ 预览已采纳版本展示"
```

---

## Phase 4：D 组 · 笔记页（9）

### Task 12: 树形合并 + 大纲收起（9️⃣①）

**Files:**
- Modify: `app/src/pages/NotesPage.tsx`（左栏合并编排）
- Modify: `app/src/components/NoteGroupPanel.tsx`（组节点化——含笔记叶子）
- Modify: `app/src/components/NoteListView.tsx`（平铺模式保留：过滤时退化）
- Modify: `app/src/components/NoteReadingView.tsx`（大纲折叠）

- [ ] **Step 1: 设计树数据结构（前端类型）**

`app/src/types/notes.ts` 或组件内：
```ts
type TreeEntry =
  | { kind: "all"; notes: Note[] }                    // 全部笔记根（平铺）
  | { kind: "group"; group: NoteGroup; notes: Note[]; expanded: boolean };
```
NotesPage 组合：`groups` + `notes` → 树（组节点含组内笔记；未归组笔记放"全部笔记"根）；`groupFilter` 语义保留（点击组节点 = 展开 + 过滤）。

- [ ] **Step 2: 写失败测试（树组件状态机）**

新建 `app/src/components/NoteGroupPanel.test.tsx`（vitest——先查 `app/vitest.config.ts` 与现有组件测试写法，跟随既有模式）：
```tsx
it("展开组节点显示组内笔记叶子", () => { /* mock groups/notes → 渲染 → 点击展开 → 断言叶子可见 */ });
it("搜索过滤时退化为平铺列表", () => { /* keyword 非空 → 断言树退化为平铺 */ });
```

- [ ] **Step 3: 实现**

- `NoteGroupPanel.tsx`：组节点渲染树结构（组头 = 现有组卡 + 展开三角；组内 = 笔记行复用 NoteListView 行样式；"全部笔记"= 根节点平铺所有笔记）；组头保留计数/徽标/复习按钮/改判入口（现有 props 不动，仅布局改造）；`onGroupFilterChange` 点击组头即切换
- `NotesPage.tsx`：`visibleNotes` 语义调整——组节点内笔记由树组件内部过滤（客户端，零请求保持）；搜索/标签过滤时传平铺模式（退化为现有 NoteListView 渲染）
- `NoteReadingView.tsx`：大纲区加折叠按钮（state `outlineCollapsed`；折叠后大纲隐藏 + 悬浮"📑"按钮展开）；布局宽度释放给正文

- [ ] **Step 4: 验证**

运行：`cd app; npx vitest run NoteGroupPanel` 通过；`npx tsc --noEmit`；`npm run build`。
手工：组展开/折叠/过滤退化/改判入口可用；大纲折叠/展开。

- [ ] **Step 5: 提交**

```bash
git add app/src/pages/NotesPage.tsx app/src/components/NoteGroupPanel.tsx app/src/components/NoteListView.tsx app/src/components/NoteReadingView.tsx app/src/components/NoteGroupPanel.test.tsx
git commit -m "feat(notes): 组与笔记列表树形合并 + 大纲可收起"
```

### Task 13: 笔记图像缩略图（9️⃣③）

**Files:**
- Modify: `app/src/components/NoteImage.tsx`（缩略图尺寸 + 点击放大）
- Modify: `app/src/components/NoteMarkdown.tsx`（img 渲染接线 onOpen）
- Modify: `app/src/components/ImagePreviewOverlay.tsx`（确认 props——复用现有）

- [ ] **Step 1: 写失败测试（缩略图渲染）**

`NoteImage.test.tsx`（或并入现有组件测试）：
```tsx
it("渲染 240px 缩略图样式", () => { /* 渲染 NoteImage → 断言 img 有 width 240/objectFit cover 样式 */ });
it("点击触发 onOpen 原图", () => { /* fireEvent.click → 断言 onOpen 被调用且传原图 URL */ });
```

- [ ] **Step 2: 运行测试确认失败**

运行：`cd app; npx vitest run NoteImage` 预期 FAIL（现渲染原图）。

- [ ] **Step 3: 实现**

`NoteImage.tsx`：默认尺寸改 `width: 240, height: 160, objectFit: "cover", cursor: "zoom-in"` + `onClick={() => onOpen?.(url, alt)}`（url 为 resolve 后的原图 URL——现有 resolve 逻辑保留）；`NoteMarkdown.tsx`：img 组件处传 `onOpen`（NotesPage 已有 `ImagePreviewOverlay` 挂载与打开状态，接线即可）。

- [ ] **Step 4: 验证**

运行：`npx vitest run NoteImage` 通过；tsc/vite 通过。手工：笔记内图缩略显示 → 点击放大 → 关闭。

- [ ] **Step 5: 提交**

```bash
git add app/src/components/NoteImage.tsx app/src/components/NoteMarkdown.tsx
git commit -m "feat(notes): 笔记图片缩略图 + 点击原图查看"
```

### Task 14: 自动归组感知优化（9️⃣②，分支取决于 Task 0 核实）

**Files:**
- 若"路由信号不足"分支：Modify `app/src-tauri/src/group_route.rs` + `note_group_assign.rs`（信号扩展——与 Task 7 领域信号联动）
- 若"UI 感知"分支：Modify `app/src/components/NoteGroupPanel.tsx`（新入组高亮）+ `app/src/pages/SessionsPage.tsx`（转笔记 toast 联动）+ `app/src/App.tsx`（lastConvertedNoteId 传递）

- [ ] **Step 1: 按 Task 0 结论选分支**

- **信号不足分支**：`group_route.rs` 的 `GroupRouteSignals` 增领域信号（domain_kind 来自 Task 7 增强后的检测）+ 结构密度信号加权调整——先写失败测试（低结构+领域命中 → TopicGroup 而非 NeedConfirm）再实现
- **UI 感知分支**：组树中"最近入组"标记（NotesPage 从转笔记事件收集 id，组节点闪烁角标 5s）；SessionsPage 转笔记成功 toast 后向 NotesPage 传递（最小实现：App.tsx 存 `lastConvertedNoteId` 传给 NotesPage）

- [ ] **Step 2: 实现 + 测试**

按分支写测试（路由：`cargo test group_route`；UI：`npx vitest run NoteGroupPanel` 增"新入组角标"用例）。

- [ ] **Step 3: 提交**

```bash
# 信号分支：
git add app/src-tauri/src/group_route.rs app/src-tauri/src/note_group_assign.rs
git commit -m "feat(groups): 归组路由信号增强（领域/结构密度联动）"
# 或 UI 分支：
git add app/src/components/NoteGroupPanel.tsx app/src/App.tsx app/src/pages/SessionsPage.tsx
git commit -m "feat(notes): 会话转笔记新入组高亮提示"
```

---

## 全量验证（Phase 4 完成后）

- [ ] **Rust**：`cd "d:\Program own\aicode\work space\Entropydecrease\app\src-tauri"; cargo test` 全绿；`cargo clippy --all-targets` 零错误（新增代码 ≤300 行/文件，超限拆文件——Task 3/8/11 已预留拆分）
- [ ] **前端**：`cd app; npx tsc --noEmit`、`npm run build`、`npx vitest run` 全绿
- [ ] **手工验收清单**（spec §测试计划）：采集中改档案热切换；PPT 同构页不再被过滤；会话 38 水印被过滤；精修工作台全流程（对比/采纳/版本回滚）；窗口展开刷新；会话编号删除归位；自动归组落库率核实（Task 0 已做）
- [ ] **文档同步**：v0.11.md 的 v0.11.5 状态更新为"已交付"；CHANGELOG.md 增 v0.11.5 记录；spec 状态行更新

## 执行记录（各任务完成后追加）

- **Task 0（2026-08-22）**：归组落库率核实——classroom 笔记 11 条 / 已归组 0 / 未归组 11（note_groups 表 0 行）→ 9️⃣② 分支选择：**路由信号不足（信号增强）**；
  样本标题无系列模式（"全11讲""你长大了…"无 P/第X集/EP 命中）+ 无领域词 → resolve_group_for_session 失败静默降级。
- cargo test 环境：**确认受阻**（0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND，System32 内置 onnxruntime 1.17 与本地 1.28 加载期冲突；ORT_DYLIB_PATH/补 providers DLL 均无效——v0.11.4 同问题）；
  **兜底方案**：纯函数模块 `rustc --test` 独立编译验证（只依赖 std/serde 的模块可直接编译）；数据层/命令层 `cargo test --no-run` 编译验证 + 逻辑审查；前端 vitest 正常。
  **注意**：`app_lib_tests` 测试目标（cargo test --no-run 构建）可运行——最终全量 1497 passed / 0 failed。

## 完成记录（2026-08-22 全部交付）

| Task | 提交 | 审查 |
|------|------|------|
| 1 display_no | d71da59 + 3cc2c55（修复） | spec ✅ + quality ✅ |
| 2 新颖度变化区域 | 97604b7 + 20ea422（修复） | spec ✅ + quality ✅ |
| 3 水印 A+B+C | 16a0a0c | spec ✅ + quality ✅ |
| 4 窗口刷新 | ecf0fa4 | 控制器直实现 |
| 5 记忆四象限 | 1d8a562 | spec ✅ + 控制器自审 |
| 6 热切换+重评 | 351dab2（补提交） | 控制器核查 |
| 7 识别率增强 | 6de6729 | 控制器核查 |
| 8 锚点剥离 | 52e7b6e | 控制器核查 |
| 9 画面要点移出+术语表 | 032a5d0 | 控制器核查 |
| 10 产物下线+精修懒自动 | 9541288 | 控制器核查 |
| 11 精修工作台 | 546dc67 + d896d3f | 控制器核查 |
| 12 树形合并+大纲 | 5227897 | 控制器核查 |
| 13 缩略图 | 85e8c73 | 控制器直实现 |
| 14 归组信号增强 | a171812 | 控制器核查 |
| clippy 清理 | edc5110 | — |
| 测试修复 ×3 | 618644b | — |
| 终审 I-1 修复 | 68d8684 | — |

- **最终验证**：cargo test 1497 passed / 0 failed；tsc 零错误；vitest 43 通过；vite 构建通过；终审 Approved（11 项全覆盖、零回归）
- **遗留**：真机验收（采集中改档案热切换、PPT 同构页、会话 38 水印、精修工作台全流程、窗口展开刷新、会话编号删除归位）
