# v0.19.7 · 用户反馈批二：开关入弹层/精修加速/挂体系轻建/列表交互重构

> 状态：**已实施（2026-09-05，代码提交 c0c6eff/1c7f808/d7d589b5/a5c912d/b6a00a4/305d8cc0/d001c2fc/33aff9a/7460ae3；真机验收待执行）**。
> 依据：用户 2026-09-05 反馈批裁决；设计：[2026-09-05-v0.19-feedback-batch-design.md（[ ] 已归档）](../superpowers/specs/2026-09-05-v0.19-feedback-batch-design.md) §2.3/2.8/2.5/2.6 + §2.10（REQ-291 用户追加）。
> 排期注：v0.19.7 承接 v0.19.6 的 REQ-284/286/287 + REQ-290 ①②（v0.19.6 已交付 ③ 埋点与其余 REQ）。

## 范围与需求映射

| REQ | 需求 | 优先级 | 状态 |
|-----|------|--------|------|
| REQ-284 | 画面理解开关入精修发起弹层（任务级覆写+设为默认单向写回） | P1 | **已实施（c0c6eff）** |
| REQ-290 | AI 精修加速：① 片内流式（NDJSON 逐节 SSE+Delta） + ② 输出预算化（③ 埋点已于 v0.19.6） | P1 | **已实施（②1c7f808 · ①b6a00a4）** |
| REQ-286 | 挂体系弹层搜索+三类内联轻建 | P1 | **已实施（d7d589b5）** |
| REQ-291 | 视频暂停随播随停（声画双通道，2026-09-05 追加） | P1 | **已实施（a5c912d）** |
| REQ-287 | 笔记列表交互重构（交互矩阵） | P1 | **已实施（305d8cc0/d001c2fc/33aff9a/7460ae3）** |

## 口径与决策记录

- **REQ-284（c0c6eff）**：见上文 §1（checkbox 初值=全局；覆写缺省不发键；设为默认单向写回；resolve_vision_refine 纯函数 2 单测 + FE 3 契约用例）。
- **REQ-290①（b6a00a4）**：NDJSON 逐节流式默认路径（env REFINE_STREAM_NDJSON=0 可关；mock 恒非流式）——`NDJSON_SYSTEM_SUFFIX` 引导逐节 JSONL + 整包自动兼容；每节解析即推 `Delta` 帧（渲染 md 片段=前缀渲染终稿同源）；模型未遵守 → 同拍回退非流式（逐字节兼容）、重试拍非流式；FE `sliceStreamContent`（delta 拼接/blockDone 收尾去重/乱序按片升序）+ TaskThreadCard 打字机（▍ 光标）。协议校验/落库/审计零变化；3 解析单测 + FE 4 归集用例。
- **REQ-290②（1c7f808）**：见前文 §1。
- **REQ-286（d7d589b5）**：见前文 §1（LinkEntityPicker 搜索树列表+三类轻建即建即选；零新后端；14 用例含 3 轻建）。
- **REQ-291（a5c912d）**：见 §2.10 落实——media_state 纯状态机（5 单测）；媒体声音戳（RMS≥0.003 200ms/块，区别于 VAD 语音级）；worker 独立 1s 媒体拍（idle 静默期亦判）+ 快恢复（≤~1.5s，OCR 5s 降级辅助）；事件/徽标；watchdog 天然互斥（暂停态不判停更）。
- **REQ-287（305d8cc0/d001c2fc/33aff9a/7460ae3）**：后端=`note_orders` scope 独立表（g{id}/none 快照先删后插单事务）+ note_order_list/save/clear（校验/2000 守卫/notes 域广播）；前端=去 checkbox（✓ 前缀+靛蓝多选视觉）、三通道多选（Ctrl 加减/Shift 列表位置区间/批量选择模式 Esc 完成）、批量栏（删除+移动到组+完成）、拖拽矩阵（行→左栏组行与中部组头双 drop[多选 JSON+单 id 兜底]、同 scope 行落点=自动启用快照+整表覆写、自动排序组仅归组）、组头空白划选（区间=组内首行至当前行）、组头「手排 ↺」一键回自动、右键选集批菜单；树视图消费手动序；组折叠只隐 body 不卸载组头（chevron 恒可再点）。12 列表交互用例 + 纯逻辑 4 用例全绿。

## 真机验收待办（登记）

- [ ] REQ-284：会话精修弹层开/关/设为默认三路径 + 全局开关联动；笔记精修不出现
- [ ] REQ-290①：抖音/B站真实会话 NDJSON 流式逐节打字机观感；模型不守约时回退非流式无感；流式 + 预算后 A/B 总耗时（v0.19.6 基线已取）
- [ ] REQ-291：抖音播放→暂停→恢复三拍：随停 ≤~2s、恢复 ≤~1.5s、徽标显隐；阈值滞回标定一次（MEDIA_AUDIO_ACTIVE_RMS/拍数）
- [ ] REQ-286：空体系/空节点/零命中新建三场景走查
- [ ] REQ-287：交互矩阵逐格真机走查（修饰多选/批量模式/组头与行拖放/手排切换与回自动/划选/右键批菜单）+ 引用跳转回归

## 验证记录

- FE：vitest **全量绿（571 用例/78 文件——含新增 REQ-284 3 例、流式归集 4 例、REQ-286 14 例、REQ-287 交互 12+4 例、选择器纯逻辑 4 例）**；tsc 零错误。
- Rust：cargo check --all-targets 干净；clippy 待终检登记（受限环境说明同 v0.19.6——Rust 单测二进制无法在本会话沙箱启动，media_state 5/协议 3/预算 4/排序 scope 1/选择? 等单测随文件入库，常规终端/CI 补跑登记）。
