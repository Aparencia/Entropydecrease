# v0.19.7 · 用户反馈批二：开关入弹层/精修加速/挂体系轻建/列表交互重构

> 状态：**实施中（2026-09-05 裁决立项；已交付 REQ-284 + REQ-290②）**。
> 依据：用户 2026-09-05 反馈批裁决；设计：[2026-09-05-v0.19-feedback-batch-design.md（[ ] 已归档）](../superpowers/specs/2026-09-05-v0.19-feedback-batch-design.md) §2.3/2.8/2.5/2.6。
> 排期注：v0.19.7 承接 v0.19.6 的 REQ-284/286/287 + REQ-290 ①②（v0.19.6 已交付 ③ 埋点与其余 REQ）。

## 范围与需求映射

| REQ | 需求 | 优先级 | 状态 |
|-----|------|--------|------|
| REQ-284 | 画面理解开关入精修发起弹层（任务级覆写+设为默认单向写回） | P1 | **已实施（c0c6eff）** |
| REQ-290 | AI 精修加速：① 片内流式 + ② 输出预算化（+③ 埋点已于 v0.19.6） | P1 | ② 已实施（1c7f808）；① 实施中 |
| REQ-286 | 挂体系弹层搜索+三类内联轻建 | P1 | 已立项（实施中） |
| REQ-287 | 笔记列表交互重构（交互矩阵） | P1 | 已立项 |

## 口径与决策记录（随实施填）

- **REQ-284**：checkbox 初值=全局；勾选=本次覆写（仅会话级显示，笔记级纯文本语境隐藏）；`ai_refine_start` 增 `vision_refine: Option<bool>`（缺省 None=后端跟随全局——payload 无键，无双写歧义）；「设为默认」显式 `ai_set_vision_refine` 写回（单向同步）；生效来源文案实时展示（本次/全局）；开启附耗时费用提示。resolve 纯函数 `resolve_vision_refine`（ai_settings.rs）+ 2 单测；FE 3 契约测试（覆写携带/未覆写无键/笔记级隐藏）。
- **REQ-290②**：`refine_budget.rs` 纯函数——guidance ≈ 输入×档位缩放（faithful 1.0 / standard·custom 0.9 / deep 0.95 / minimal 0.3，下限 200 字）；max_tokens = guidance×2+1500，下限 4000（防截断毁 JSON 校验）、档位上限（16k / minimal 9k，恒 ≤ 原 20k）；system 尾追加引导段（动态逐片值，静态预览不含——注释如实声明）；adapter 以 clone 客户端覆盖上限，协议/校验/轨迹零变化。4 单测入库。

## 真机验收待办（登记）

- [ ] REQ-284：会话精修弹层开/关/设为默认三路径 + 全局开关联动；笔记精修不出现
- [ ] REQ-290：3 条真实会话 A/B（v0.19.6 已取基线；预算化后对比耗时/字数/成本）→ 流式上线后再取一组

## 验证记录（随实施填）

- v0.19.6 底：tsc 零错误 · vitest 75 文件 556 用例全绿 · clippy 零警告（受限环境说明见 v0.19.6 文档）
- REQ-284：RefineLaunchDialog 契约 + 新 3 用例全绿（5/5）
- REQ-290②：refine_budget 4 单测入库（待常规终端/CI 补跑——受限环境说明同 v0.19.6）
