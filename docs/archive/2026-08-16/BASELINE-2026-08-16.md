# 课堂识别评估基线报告（2026-08-16）

> 用途：P0-1 识别评估基线的**算法自检与口径确认**。真实语料基线待 `corpus/manifest.json` 准备完成后由 `node scripts/asr-eval/eval.mjs` 生成，运行结果写入 `results/report-*.json`，并按本报告「基线记录」一节回填。
> 口径说明：厂商「98% 准确率」为干净语料宣传口径；真实课堂 CER 常见 5-20% 属正常（市场调研：`docs/Foresight/asr-market-2025-2026.md` §6）。本基线以**相对改进**为验收目标，不对标宣传口径。

## 一、算法自检结果（--self-test）

自检覆盖 CER 编辑距离与归一化、热词命中/未命中/notInRef 口径。运行命令：

```bash
cd client && node scripts/asr-eval/eval.mjs --self-test
```

预期输出：全部 `[self-test] PASS`，退出码 0（CI 门禁）。

## 二、基线记录（真实语料，待运行回填）

按 `client/scripts/asr-eval/corpus/README.md` 准备 10 节课语料（5 网课 + 5 技能）后执行：

```bash
cd client && node scripts/asr-eval/eval.mjs --cloud   # 或 --file / --local
```

| 字段 | 记录值 |
|---|---|
| 运行日期 | （回填） |
| 引擎 | （回填：gateway-cloud / local-sherpa / pre-recorded） |
| 聚合 CER | （回填，%） |
| 热词命中率 | （回填，%） |
| 高频未命中术语 Top 5 | （回填） |

## 三、后续验收对照

| 任务 | 验收指标 | 对比方式 |
|---|---|---|
| P0-4 重复修复 | 相邻 final 重复率 <0.5%；CER 不劣化 | 与会话日志对比 + 本基线 |
| P0-5 CPU 优化 | CPU 峰值下降 ≥50%；CER 不劣化 | 进程采样 + 本基线 |
| P0-6 准确率即时提升 | 术语热词命中率 ≥80% | 本基线 |
| P1-1 两遍重打分 | CER 相对下降 ≥10% | 本基线 |
| P1-2 动态热词 | 热词命中率提升 | 本基线 |
