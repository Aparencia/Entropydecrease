{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "capture-float"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
