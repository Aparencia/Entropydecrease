{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "capture-float", "capture-overlay"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
