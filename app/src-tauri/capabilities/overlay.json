{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture-overlay",
  "description": "Capability for the capture overlay window (v0.12.3: 最小权限——覆盖层仅框选交互与窗口自身操作，无 dialog)",
  "windows": ["capture-overlay"],
  "permissions": [
    "core:default"
  ]
}
